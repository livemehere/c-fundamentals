# char

```c
char a[] = "hello";
char* b = "hello";

printf("%zu\n", sizeof(a)); // 6
printf("%zu\n", sizeof(b)); // 8
```

문자열을 선언할 때 배열 vs 포인터에 차이가있다.   

- 배열 : stack 에 저장되어 메모리 자체를 소유한다.
- char 포인터 : 주소만 저장하고, 데이터는 다른곳에 저장된다.

배열은 당연하게도 "컴파일 타임" 에 결정된다. 그리고 일반적으로 정리하면 배열은 수정가능, 포인터는 수정 불가능하다.
